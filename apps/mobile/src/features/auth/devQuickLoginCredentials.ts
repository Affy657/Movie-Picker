/**
 * Compte principal de démo (README — DevelopmentSeed). Aligné sur le web.
 * Utilisé uniquement par le bouton de connexion rapide ; n'invoque ce module
 * que sous `__DEV__` (sinon Metro l'embarque dans le bundle release).
 */
export const DEV_QUICK_LOGIN_EMAIL = 'dev@test.local';
export const DEV_QUICK_LOGIN_PASSWORD = 'DevTest123!';
