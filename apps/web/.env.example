# URL de l'API (injectée au build). En dev, défaut http://localhost:4000
# VITE_API_URL=http://localhost:4000

# Production (déploiement AWS) : pointer vers l'URL Cloud Run
# VITE_API_URL=https://votre-service.run.app
