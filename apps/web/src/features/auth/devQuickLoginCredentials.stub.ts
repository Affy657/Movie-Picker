/**
 * Substitué en build production (alias Vite) — aucun identifiant de démo dans le bundle livré.
 * @see vite.config.ts
 */
export const DEV_QUICK_LOGIN_EMAIL = '';
export const DEV_QUICK_LOGIN_PASSWORD = '';
