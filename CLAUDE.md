# CLAUDE.md

Toutes les règles du dépôt sont dans @AGENTS.md — les lire avant toute modification.

Ce fichier n'existe que parce que Claude Code ne découvre pas `AGENTS.md` tout seul : sa liste de fichiers d'instructions est câblée sur `CLAUDE.md`. Ne rien écrire ici, toute règle va dans `AGENTS.md`, que les autres agents (Codex, Cursor, Gemini CLI, Copilot) lisent directement.
